main()
{
	printf("Hello, world/n");  /*because the headers stdio.h and stdlib are used frequently, so though I leave out the header, 
								the compiler will not produce an error, and still execute as usual*/
}